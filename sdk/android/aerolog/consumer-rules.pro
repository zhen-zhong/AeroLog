# AeroLog SDK consumer ProGuard rules.
#
# The SDK uses Room and OkHttp. Room's compiler generates the required keep
# rules for database implementations; this file is intentionally small and
# exists so consumerProguardFiles always resolves in host projects.
