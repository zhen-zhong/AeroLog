- 启动命令：在 `deploy/` 目录下执行 `docker compose up -d`。
- 默认凭证：各组件（DB、MQ、对象存储）均预设了统一的开发环境账号密码（如 `aerolog/aerolog`），详见 `docker-compose.yml` 的 `environment` 字段。
- 监控接入：Prometheus 配置为从 `host.docker.internal` 的 9101-9103 端口采集指标，适用于本地开发调试场景。