- **启动命令**：在 `deploy/` 目录下执行 `docker compose up -d` 即可拉起全套基础设施。
- **健康检查**：Compose 文件中为 Postgres、Redis、ClickHouse 配置了 `healthcheck`，确保依赖服务就绪。
- **端口映射**：业务侧需注意端口冲突，如 MinIO API 映射为 9002，Grafana 映射为 3001，Redpanda Console 映射为 8088。