- 所有 Handler 均定义 `Register(*gin.RouterGroup)` 方法以集中注册路由。
- API 响应统一封装为 `gin.H{"data": ...}` 或 `gin.H{"err": ...}` 格式。
- SQL 查询优先使用参数化占位符（PostgreSQL 用 `$N`，ClickHouse 用 `?`）以防止注入。
- Handler 结构体直接持有数据库连接池（`*pgxpool.Pool` 或 `driver.Conn`）作为字段。