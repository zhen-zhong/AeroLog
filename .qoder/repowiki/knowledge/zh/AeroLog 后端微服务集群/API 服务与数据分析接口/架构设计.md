- 采用 Gin 框架构建 HTTP 服务，通过 `internal/app` 进行依赖注入与生命周期管理。
- 路由层直接调用 `internal/handler` 中的处理器，处理器内联 SQL 逻辑，分别操作 PostgreSQL（元数据/配置）和 ClickHouse（事件分析）。
- 配置通过 `internal/config` 从环境变量加载，支持 CORS 及多数据库连接参数。
- 集成 Prometheus 指标采集中间件，监控请求耗时与总量。