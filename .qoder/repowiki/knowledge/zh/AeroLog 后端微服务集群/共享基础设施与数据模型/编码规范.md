- 所有 Prometheus 指标（Counter, Histogram, Gauge）在创建时自动注册到全局单例 `Registry`，避免手动注册遗漏。
- Kafka 生产者采用异步非阻塞模式，并在初始化时启动后台 goroutine 消费 `Errors()` channel 以防止阻塞。
- 事件模型结构体提供标准化的序列化方法（如 `MarshalKafka`）及基础字段校验逻辑（`Validate`）。