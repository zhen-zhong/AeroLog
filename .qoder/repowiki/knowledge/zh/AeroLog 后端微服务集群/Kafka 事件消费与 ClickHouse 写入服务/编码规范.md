- 采用分层架构设计，通过 `internal/app` 集中管理组件初始化与依赖注入，实现业务逻辑与基础设施解耦。
- 在批量处理流程中集成 Prometheus 监控，对消费总量、Flush 耗时、批大小及 DLQ 数量进行多维度指标采集。
- 使用 `pgx.Batch` 机制将元数据同步操作合并发送，减少与 Postgres 的网络交互次数以提升性能。
- 遵循健壮性设计原则，对解析失败或写入异常的消息统一转入 Postgres `event_dlq` 表并记录错误原因。