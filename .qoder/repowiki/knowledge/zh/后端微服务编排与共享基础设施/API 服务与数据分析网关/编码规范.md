- 统一的 JSON 响应格式：所有接口均返回包含 `data` 字段的成功响应或包含 `err` 字段的错误响应（如 `gin.H{"data": ...}` 或 `gin.H{"err": ...}`）。
- Handler 直连数据库模式：Handler 结构体直接持有数据库连接池指针，并在方法内部通过字符串拼接或参数化查询直接操作数据库，省略了 Service/Repository 抽象层。
- 基于环境变量的配置加载：`config.FromEnv()` 统一从环境变量读取配置并提供默认值，覆盖 API 端口、数据库 DSN 及 JWT 密钥等核心参数。