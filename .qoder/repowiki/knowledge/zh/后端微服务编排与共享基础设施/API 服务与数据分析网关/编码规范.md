- 统一响应格式：所有接口均返回包含 `data` 或 `err` 键的 JSON 对象。
- 动态 SQL 构建：在 Handler 中根据查询参数动态拼接 SQL 语句及参数列表。
- 自动表结构同步：在查询前通过 `ensure...` 方法自动执行 DDL 以确保数据库表结构存在。
- 原始字节解析：使用 `json.Unmarshal` 处理数据库中存储的 JSONB 或字符串化 JSON 字段。