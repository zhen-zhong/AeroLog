- 采用基于环境变量的配置加载模式，所有敏感信息与地址均通过 `config.FromEnv()` 统一获取。
- 数据库写操作广泛使用 `pgx.Batch` 或 ClickHouse `PrepareBatch` 进行批量聚合，以降低网络往返开销。
- 在处理用户画像（Profile）事件时，采用“先读后写”的快照合并策略，确保 `users` 表数据的最终一致性。
- 引入 Metrics 埋点（Counter/Histogram）监控消费总量、Flush 耗时及 DLQ 积压情况，便于运维观测。