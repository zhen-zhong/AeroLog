- 采用 Go Workspace (`go.work`) 将 `api`、`collector`、`consumer` 及 `pkg` 聚合为单一开发单元，实现跨模块依赖的本地解析。
- 所有业务服务均通过 `replace` 指令强制依赖本地 `server/pkg`，确保事件结构 (`model/event.go`)、Kafka 生产者封装及监控指标定义的绝对一致性。
- 各服务保持独立的 `cmd/main.go` 入口与配置体系，通过共享的基础设施层解耦业务逻辑与底层通信协议（Kafka/PostgreSQL/ClickHouse）。