- 所有页面组件均声明为 "use client"，以支持客户端交互和状态管理。
- 统一使用 `api.listProjects()` 获取项目列表，并在 `useEffect` 中自动选中首个项目作为默认上下文。
- 时间范围状态统一使用 `[Dayjs, Dayjs]` 元组表示，并通过 `dayjs` 进行初始化和格式化。
- 图表配置对象通过 `useMemo` 包裹，仅在相关数据或维度变化时重新计算，以提升渲染性能。