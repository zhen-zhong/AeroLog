- **样式引擎**：Tailwind CSS 配合 `clsx` 和 `tailwind-merge` (`lib/utils.ts`) 实现动态类名合并。
- **组件变体**：使用 `class-variance-authority` (CVA) 管理组件的多态样式（如 Button 的 variant/size）。
- **图标库**：集成 `lucide-react` 提供统一风格的矢量图标。