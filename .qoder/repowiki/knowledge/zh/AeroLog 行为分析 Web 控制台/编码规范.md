- 所有业务页面均采用 Next.js App Router 的 `page.tsx` 约定进行路由定义
- API 请求统一通过 `src/lib/api.ts` 中的 `req` 辅助函数发起，并自动附加 `/v1` 前缀
- UI 组件库基于 shadcn/ui 规范构建，统一存放于 `src/components/ui` 目录
- 全局状态管理采用 Zustand 模式，Store 定义集中于 `src/stores` 目录