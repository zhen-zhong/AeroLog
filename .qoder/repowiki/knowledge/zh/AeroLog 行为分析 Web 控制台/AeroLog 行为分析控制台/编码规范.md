- 所有分析页面均采用 "use client" 指令，并在顶层使用 `AnalyticsHeader` 定义页面标题与描述。
- 统一使用 `dayjs` 处理时间范围选择，默认初始化为最近 7 天或特定业务周期。
- 通过 `useProjectStore` 获取 `projectId`，并作为所有 API 请求的首要路径参数。
- 数据加载状态统一通过 `@tanstack/react-query` 的 `isLoading/isFetching` 标志驱动 UI 骨架屏或加载动画。