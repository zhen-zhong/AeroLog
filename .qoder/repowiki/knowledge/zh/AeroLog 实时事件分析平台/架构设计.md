- **全链路数据流协同**：以 `docs/event.schema.json` 为跨模块契约，驱动 SDK 采集、Collector 接收、Consumer 清洗入库及 API 查询的全流程一致性。
- **基础设施即代码化启动**：根目录 `Makefile` 结合 `deploy/docker-compose.yml` 提供一键式环境拉起能力，自动完成 PostgreSQL、ClickHouse 等中间件的 Schema 初始化与服务依赖编排。
- **共享模型与通信标准**：后端各微服务通过 `server/pkg` 共享 Go 模块，确保事件数据结构（`model/event.go`）与消息队列生产逻辑在采集与消费侧的严格对齐。