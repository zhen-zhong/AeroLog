- **全链路数据流**：多端 SDK 遵循统一协议上报至 Collector，经 Redpanda 缓冲后由 Consumer 写入 ClickHouse，最终由 API 服务提供查询支撑。
- **共享契约治理**：后端通过 `server/pkg` 共享数据模型与通信组件，前端与 SDK 严格对齐 `docs/event.schema.json` 确保跨层数据结构一致。
- **一体化基础设施**：利用 Docker Compose 统一编排存储与消息中间件，并通过预置 SQL 脚本与 Grafana 面板实现环境初始化与可观测性同步。