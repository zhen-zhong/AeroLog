- 所有后端微服务（api, collector, consumer）均遵循统一的内部目录结构规范（internal/app, internal/config, internal/handler）。
- 多端 SDK（Android, iOS, Web）与后端服务严格对齐 `event.schema.json` 定义的事件字段与类型。
- 基础设施配置与业务代码分离，数据库迁移脚本集中存放于 `deploy/migrations` 目录并按时间戳命名。