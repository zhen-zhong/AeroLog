- 所有页面组件均声明为 "use client" 以支持浏览器端交互逻辑。
- 统一通过 `api.listProjects()` 获取项目列表，并在首个项目可用时自动设置默认选中项。
- 使用 `dynamic(() => import("echarts-for-react"), { ssr: false })` 动态导入图表组件以禁用服务端渲染。
- 时间范围筛选统一基于 `dayjs` 对象管理，并在发起 API 请求时转换为毫秒级时间戳。