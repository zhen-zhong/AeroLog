- 采用 `AppShell` 组件作为全局布局容器，统一集成 Ant Design 样式、React Query 状态管理及侧边栏导航路由。
- 所有子模块（Console/Admin）共享 `src/lib/api.ts` 定义的统一 API 客户端，确保与后端 Go 服务通信协议的一致性。
- 通过 Next.js App Router 的 `layout.tsx` 实现根级上下文注入，使各业务页面无需重复配置基础依赖。