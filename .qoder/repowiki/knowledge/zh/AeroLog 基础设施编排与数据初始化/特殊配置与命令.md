- **启动命令**：在 `deploy/` 目录下执行 `docker compose up -d` 即可拉起全套环境。
- **健康检查**：Compose 文件中为 Postgres、Redis、ClickHouse 配置了 `healthcheck`，确保依赖服务就绪。
- **默认凭证**：各组件均预设了开发环境的统一账号密码（如 `aerolog/aerolog`），并在 PostgreSQL 中插入了初始管理员账户。