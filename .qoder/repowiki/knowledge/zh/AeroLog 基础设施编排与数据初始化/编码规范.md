- 所有持久化服务均通过 `volumes` 将数据目录映射至 `./data/<service_name>`，实现状态与计算分离。
- 关键基础设施服务（PostgreSQL, Redis, ClickHouse）均配置了基于健康检查探针的 `healthcheck` 逻辑，确保依赖服务的就绪状态。
- 数据库初始化脚本统一采用 `<序号>_<描述>.sql` 命名规范（如 `01_schema.sql`），以确保在 entrypoint 阶段的确定性执行顺序。