- 数据库初始化脚本统一采用 `NN_description.sql` 命名格式（如 `01_schema.sql`），以确保在 entrypoint 中的执行顺序。
- 所有持久化数据均通过 `volumes` 挂载至 `./data/<service_name>` 目录，实现状态数据与编排配置的物理隔离。
- 监控配置遵循“声明式”原则，Grafana 的数据源和看板均通过 YAML/JSON 文件预置，避免手动 UI 配置。