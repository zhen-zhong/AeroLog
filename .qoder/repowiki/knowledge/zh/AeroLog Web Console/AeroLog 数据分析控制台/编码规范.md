- 所有页面组件均使用 "use client" 指令，表明其为客户端渲染组件。
- 统一使用 `dayjs` 处理时间范围状态，并默认初始化为最近 7 天或特定业务周期。
- 在 `projectId` 变更时，通过 `useEffect` 强制重置模块内的局部筛选状态（如事件选择、日期范围），防止数据污染。
- 采用“左侧配置/筛选 + 右侧可视化/结果”的双栏响应式布局模式（`xl:grid-cols-[..._minmax(0,1fr)]`）。