- **路由隔离**：采用 Next.js App Router 目录结构，将 `admin`（管理配置）与 `console`（数据分析）划分为独立的路由分支，实现功能模块的物理隔离。
- **共享基础设施**：通过 `src/lib/api.ts` 提供统一的 API 客户端封装，屏蔽底层 Go 服务通信细节；利用 `src/stores/project-store.ts` 实现跨页面的全局项目上下文（Project Context）持久化。
- **组件复用体系**：依托 `src/components` 下的通用布局（Shell/Header）与基础 UI 库，确保管理界面与分析看板在视觉与交互上的一致性。