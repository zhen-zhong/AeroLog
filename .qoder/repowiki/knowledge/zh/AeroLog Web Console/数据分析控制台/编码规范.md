- 所有页面组件均声明为 "use client"，并在组件内部通过 `useState` 和 `useEffect` 管理项目选择、时间范围等本地状态。
- 普遍采用“左侧配置/列表 + 右侧详情/图表”的双栏响应式布局结构（通常使用 `xl:grid-cols-[width_minmax(0,1fr)]`）。
- 数据加载状态统一通过 `MetricTile` 或 `Skeleton` 组件展示，空状态则统一使用 `EmptyAnalysis` 组件进行提示。
- 图表容器统一使用 `ChartPanel` 组件包裹，以保持标题、描述及边框样式的标准化。