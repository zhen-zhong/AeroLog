- **分层结构**：模块划分为 `model`（数据契约）、`mq`（消息中间件适配）和 `metrics`（监控基础设施）三个独立子包。
- **依赖方向**：各子包均为无状态或轻量封装，不相互依赖，由上层业务服务（如 collector/consumer）按需引入。
- **关键接口**：`model` 定义了 `Event` 与 `EnvelopedEvent` 作为跨服务流转的统一数据结构；`mq` 基于 `sarama` 提供异步 `Producer`；`metrics` 维护全局 `Registry` 并提供独立的 HTTP Server 暴露 `/metrics` 端点。