- **统一工作空间管理**：采用 `go.work` 将 `api`、`collector`、`consumer` 及 `pkg` 聚合为单一开发单元，确保依赖版本一致性与跨模块引用便利。
- **共享基础设施层**：所有子服务均依赖 `server/pkg` 提供的核心能力，包括统一的 Kafka 生产者封装、Prometheus 指标暴露接口以及标准化的事件数据模型。
- **标准化启动范式**：各服务入口（`cmd/main.go`）遵循一致的初始化流程，即加载配置、建立数据库连接、注册业务逻辑并启动独立的 Metrics HTTP 服务器，形成统一的运维边界。