- 采用 Go Workspace (`go.work`) 统一管理 `api`、`collector`、`consumer` 及 `pkg` 四个子模块，实现本地依赖替换与协同开发。
- 所有业务服务均通过 `replace` 指令强制依赖 `server/pkg`，确保事件结构 (`model/event.go`)、Kafka 生产者封装及 Prometheus 指标定义的跨服务一致性。
- 形成以 Kafka 为异步通信骨干、ClickHouse/Postgres 为持久化层的流式数据处理架构，各服务职责隔离但共享底层技术契约。