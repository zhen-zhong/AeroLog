- 消息队列：使用 `github.com/IBM/sarama` 消费 Kafka/Redpanda，配置 `BalanceStrategyRange` 与 `OffsetOldest`。
- 分析数据库：使用 `github.com/ClickHouse/clickhouse-go/v2`，开启 `async_insert` 提升批量写入吞吐。
- 关系数据库：使用 `github.com/jackc/pgx/v5` 维护元数据字典及死信队列（DLQ）。