- 采用指标埋点模式：在关键路径（如消费计数、Flush 耗时、DLQ 数量）使用 `pkg/metrics` 暴露 Prometheus 指标。
- 遵循健壮的类型转换惯例：在处理动态属性（Properties）时，通过 `pickStr`、`pickU16` 等辅助函数安全地从 `map[string]interface{}` 提取字段并提供默认值。
- 实施批量处理优化：在 Metadata Syncer 中使用 `pgx.Batch` 聚合多个 UPSERT 操作，减少数据库往返次数。