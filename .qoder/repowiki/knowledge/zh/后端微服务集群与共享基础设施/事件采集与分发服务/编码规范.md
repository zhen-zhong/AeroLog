- 所有服务配置项（如端口、DSN、Topic）均通过 `internal/config.FromEnv()` 从环境变量读取，并提供默认值以支持本地开发。
- HTTP 处理器采用结构体方法注册模式（如 `TrackHandler.Register`），将依赖（Cache, Producer）通过字段注入，保持 handler 无状态且易于测试。
- 在关键业务路径（如事件接收、Kafka 发送、请求耗时）集成 Prometheus 指标采集，通过 `metrics.Counter` 和 `metrics.Histogram` 暴露服务运行状态。