- 入口层：`cmd/main.go` 负责信号监听与应用生命周期管理。
- 组装层：`internal/app` 负责依赖注入，初始化 PostgreSQL/ClickHouse 连接池，并注册 Gin 中间件（CORS、指标监控）。
- 业务层：`internal/handler` 采用 Handler 模式，直接处理 HTTP 请求并执行 SQL 查询。其中 `AnalyticsHandler` 利用 ClickHouse 的 `windowFunnel` 等高级函数实现复杂分析逻辑。
- 配置层：`internal/config` 通过环境变量加载运行时配置。