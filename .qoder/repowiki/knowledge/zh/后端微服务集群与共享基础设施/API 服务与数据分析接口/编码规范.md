- Handler 采用结构体定义并实现 `Register` 方法以批量注册路由，依赖通过字段注入（如 `PG *pgxpool.Pool`）。
- API 响应统一使用 `gin.H{"data": ...}` 或 `gin.H{"err": ...}` 格式包装，保持前后端交互结构一致。
- 数据库查询错误直接在 Handler 中处理并返回对应的 HTTP 状态码（如 404, 500），未引入独立的服务层或错误包装器。