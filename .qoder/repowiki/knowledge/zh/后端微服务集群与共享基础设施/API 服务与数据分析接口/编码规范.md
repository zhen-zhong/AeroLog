- Handler 结构体直接持有数据库连接池（*pgxpool.Pool 或 driver.Conn），并在 Register 方法中绑定路由。
- 所有 API 响应统一封装为 {"data": ...} 或 {"err": ...} 的 JSON 格式。
- 在 Handler 内部定义局部 struct 用于 Scan 扫描数据库结果，并直接映射为 JSON 响应字段。
- 针对分页和限制参数（limit）实施统一的边界检查与默认值填充逻辑（如 clampLimit）。