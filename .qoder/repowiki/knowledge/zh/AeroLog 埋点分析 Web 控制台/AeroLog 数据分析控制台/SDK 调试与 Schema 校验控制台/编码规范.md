- 使用 `__all__` 作为下拉选择器中表示“全部/无过滤”的约定值。
- 在 `useEffect` 中根据数据加载情况自动初始化首个可用选项（如第一个项目或属性）。
- 通过 `placeholderData` 配置 React Query 查询，在重新获取数据时保留旧内容以避免界面闪烁。
- 使用 `Badge` 组件的不同变体（success, warning, danger）直观展示数据校验结果与严重等级。