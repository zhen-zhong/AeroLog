- 使用 `placeholderData` 保持查询刷新时的界面稳定性，避免加载闪烁。
- 通过 `useEffect` 监听数据源变化，自动初始化默认选中的项目或属性。
- 采用复合键 `queryKey`（如 `["debug_events", projectId, filter]`）精确控制缓存失效与重新请求。
- 在表格渲染中使用自定义工具函数（如 `formatTime`, `propsSummary`）处理原始数据的格式化与截断。