- 采用 Next.js App Router 的单页面架构，以 `page.tsx` 为唯一入口，集成 UI 展示与业务逻辑。
- 依赖 `@tanstack/react-query` 管理异步状态，通过 `lib/api.ts` 封装的统一客户端与后端 Go 服务交互。
- 复用 `features/analytics/analytics-ui` 中的通用组件（如 `ProjectPicker`、`AnalyticsHeader`）保持界面一致性。
- 内部划分为过滤控制区、Schema 规则配置区（支持属性级与事件级规则）以及数据展示区（事件流与问题表）。