- 所有分析页面均采用 "use client" 指令，并在组件内部通过 `useState` 管理本地筛选状态（如 projectId, range）。
- 统一使用 `dayjs` 处理时间范围选择，并默认初始化为最近 7 天或 30 天的业务常用窗口。
- 页面布局遵循“左侧配置面板 + 右侧可视化结果”的双栏响应式网格结构（`xl:grid-cols-[width_minmax(0,1fr)]`）。
- 通过 `api.listProjects()` 获取项目列表，并在 `useEffect` 中自动选中首个可用项目作为默认上下文。