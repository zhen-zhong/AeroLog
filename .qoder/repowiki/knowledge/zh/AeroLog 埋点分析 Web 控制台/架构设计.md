- **统一应用外壳**：根布局 `layout.tsx` 强制包裹 `AppShell` 组件，为 `admin`（管理）和 `console`（分析）子模块提供一致的响应式导航与视觉框架。
- **全局状态注入**：通过 `providers.tsx` 在顶层注入 `TanStack Query` 客户端，实现跨路由的数据缓存、同步及错误处理机制。
- **标准化 API 契约**：`lib/api.ts` 封装了统一的 RESTful 请求逻辑（含自动鉴权头与错误拦截），作为所有子模块访问后端 Go 服务的唯一数据通道，确保接口调用的一致性。