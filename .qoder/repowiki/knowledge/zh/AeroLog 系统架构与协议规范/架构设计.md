- **核心文档体系**：包含 `architecture.md`（数据流与容量演进）、`protocol.md`（HTTP 上报接口与 SDK 行为约定）、`event.schema.json`（JSON Schema 校验标准）及 `observability.md`（Prometheus/Grafana 监控指标定义）。
- **架构逻辑**：描述了从多端 SDK 经 Collector (Go/Gin) 接入，通过 Kafka 缓冲，由 Consumer 进行 ETL 并写入 ClickHouse/Postgres 的完整链路。
- **协议规范**：明确了 `/v1/track` 接口的鉴权（Token/HMAC）、批量压缩（gzip）、离线兜底策略及预置属性/事件标准。
- **运维标准**：规定了各 Go 服务独立的 metrics 端口暴露方式及关键业务指标（QPS、Lag、DLQ）的监控与告警阈值。