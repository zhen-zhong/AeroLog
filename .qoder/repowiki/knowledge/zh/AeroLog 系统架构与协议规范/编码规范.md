- 预置属性与事件命名统一以 `$` 符号开头（如 `$AppStart`, `$os`），以区分自动采集字段与用户自定义字段。
- 所有 Go 微服务遵循独立的 metrics 端口暴露规范（非业务端口复用），并通过 `AEROLOG_METRICS_ADDR` 环境变量进行配置。
- SDK 上报协议强制要求批量处理（默认 50 条/5 秒）与 gzip 压缩，并在客户端实现基于指数退避的离线持久化重试机制。