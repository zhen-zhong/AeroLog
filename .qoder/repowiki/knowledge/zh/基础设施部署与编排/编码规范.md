- 所有数据库初始化脚本统一命名为 `01_schema.sql` 并放置在各组件对应的 `init/` 子目录中。
- 容器数据卷挂载遵循 `./data/<service_name>` 映射到容器内部标准数据路径的约定，确保本地开发环境数据持久化。
- 监控配置采用 Provisioning 机制，Grafana 的数据源和仪表盘通过 YAML 和 JSON 文件在启动时自动加载，避免手动配置。