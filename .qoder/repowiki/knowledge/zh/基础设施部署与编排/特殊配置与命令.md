- 启动命令：在 `deploy/` 目录下执行 `docker compose up -d`。
- 访问端口：Grafana (3001), Prometheus (9090), Redpanda Console (8088), MinIO Console (9001), ClickHouse HTTP (8123)。
- 默认凭据：PostgreSQL/ClickHouse/MinIO 均预设了 `aerolog` 相关的用户名和密码，Grafana 默认 admin/admin。