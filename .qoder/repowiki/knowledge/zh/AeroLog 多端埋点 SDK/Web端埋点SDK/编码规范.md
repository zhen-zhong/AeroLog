- 所有对外暴露的异步操作（如 flush, drainBuffer）均返回 Promise，支持调用方在路由切换等关键节点等待完成。
- 存储操作（localStorage/IndexedDB）均采用 try-catch 包裹，确保在隐私模式或权限受限环境下 SDK 不崩溃。
- 事件属性采集遵循 `$` 前缀约定（如 $pageview, $browser），并与服务端定义的 event.schema.json 保持结构对齐。