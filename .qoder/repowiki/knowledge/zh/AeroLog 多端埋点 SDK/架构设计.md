- **统一 API 契约**：三端均暴露 `init/setup`、`track`、`identify`、`setProfile` 等核心方法，确保业务层调用逻辑一致。
- **一致的离线策略**：各端均实现“内存批量（50条/5s）+ 本地持久化（Room/NDJSON/IndexedDB）+ 容量限制（10000条）”的兜底机制。
- **生命周期感知**：Android/iOS 监听应用前后台切换触发 flush，Web 端利用 `visibilitychange` 和 `sendBeacon` 确保页面卸载时数据不丢失。