- 所有公共 API 均通过 `lock.lock()` 配合 `defer { lock.unlock() }` 确保状态变更的原子性。
- 敏感标识符（如 `anonId`, `userId`）及配置状态统一持久化至 `UserDefaults.standard` 以实现跨会话保持。
- 自动采集的系统维度属性（如 `$os_version`, `$screen_width`）统一封装在 `autoProps` 方法中按需注入。