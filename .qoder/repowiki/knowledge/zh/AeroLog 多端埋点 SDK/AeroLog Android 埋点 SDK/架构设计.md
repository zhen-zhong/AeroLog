- 入口层：`AeroLog` 单例对象作为核心 API，负责初始化上下文、管理内存缓冲及触发上报。
- 配置层：`AeroConfig` 数据类定义服务端地址、批处理大小及自动追踪开关等运行时参数。
- 存储层：基于 Room 构建 `EventDatabase`，通过 `StoredEvent` 实体与 `EventDao` 实现事件的本地持久化、容量限制（trimOldest）及断网重试支持。
- 依赖方向：入口层直接依赖配置与存储层，并通过 `ProcessLifecycleOwner` 和 `ActivityLifecycleCallbacks` 监听应用状态变化以触发自动埋点。