- 所有耗时操作（如数据库读写、网络发送）均封装在 `CoroutineScope(Dispatchers.IO)` 中异步执行，避免阻塞主线程。
- 敏感或持久化标识（如匿名 ID、用户 ID）统一通过 `SharedPreferences` 进行本地存取，确保跨会话一致性。
- 自动采集的系统维度信息（如 OS 版本、设备型号、App 版本）统一在 `collectAutoProps` 方法中注入到事件属性中。