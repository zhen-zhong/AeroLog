- 入口层：`AeroLog` 单例对象作为核心 API，负责初始化、事件入队及公共属性管理。
- 配置层：`AeroConfig` 定义服务端地址、批量阈值及自动采集开关等运行时参数。
- 持久化层：基于 Room 的 `EventDatabase` 实现离线事件存储，包含 `StoredEvent` 实体与 `EventDao` 数据访问接口。
- 依赖方向：入口层依赖配置层与持久化层，通过 OkHttp 进行网络通信，利用 Lifecycle 组件监听应用状态。