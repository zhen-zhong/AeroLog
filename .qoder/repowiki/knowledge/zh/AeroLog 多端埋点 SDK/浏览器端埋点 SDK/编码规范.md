- 所有对外暴露的公共方法（如 track, identify）均定义在 `AeroLog` 类中，并通过 `init` 工厂函数进行实例化导出。
- 涉及浏览器特有 API（如 localStorage, navigator）的操作均包裹在 try-catch 块中，并包含环境存在性检查以增强健壮性。
- 自动采集的环境属性（如 OS、Browser、Screen）统一在 `collectAutoProps` 方法中聚合，并合并到每个事件的 properties 字段中。