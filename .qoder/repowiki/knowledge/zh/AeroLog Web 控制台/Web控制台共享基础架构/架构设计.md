- 采用Next.js App Router架构，通过`layout.tsx`定义根布局并注入`AppShell`组件。
- `AppShell`作为客户端组件（"use client"），集成Ant Design的`ConfigProvider`和TanStack Query的`QueryClientProvider`，提供全局状态管理和UI主题。
- `lib/api.ts`封装统一的HTTP请求逻辑，基于`fetch`实现与后端Go服务的通信，并定义标准化的响应接口（`ApiList`, `ApiOne`）及业务数据模型。