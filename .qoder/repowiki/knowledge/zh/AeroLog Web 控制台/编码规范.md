- 所有 API 请求统一通过 `src/lib/api.ts` 中的 `req` 辅助函数发起，自动处理 `/v1` 前缀、JSON 序列化及错误抛出。
- 业务页面按功能域拆分至 `src/features` 目录，并在 `src/app` 的路由页面中作为顶层组件引入，保持路由层轻薄。
- 通用 UI 组件集中于 `src/components/ui`，布局与结构性组件置于 `src/components/layout`，实现展示逻辑与业务逻辑的物理隔离。