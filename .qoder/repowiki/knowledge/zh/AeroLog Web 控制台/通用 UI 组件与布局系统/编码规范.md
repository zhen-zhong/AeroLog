- 所有 UI 组件均使用 `React.forwardRef` 进行包装以支持 ref 转发，并通过 `displayName` 明确组件名称。
- 组件样式统一通过 `cn()` 工具函数合并默认类名与传入的 `className`，确保 Tailwind 样式的可覆盖性。
- 业务页面（Features）采用“声明式数据获取”模式，在组件顶层使用 `useQuery` 获取数据，并配合 `Skeleton` 组件处理加载状态。
- API 客户端统一在 `lib/api.ts` 中定义，采用路径拼接方式生成 URL，并统一处理非 200 状态的错误抛出。