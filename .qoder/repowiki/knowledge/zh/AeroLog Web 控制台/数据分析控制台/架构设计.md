- 采用 Next.js App Router 架构，通过 `page.tsx` 定义四个独立的路由入口（概览、事件、漏斗、留存）。
- 内部遵循“UI 组件 + TanStack Query 数据获取”的分层模式，所有页面均依赖 `@/lib/api` 进行后端通信。
- 图表渲染采用动态导入策略（`next/dynamic`）集成 `echarts-for-react`，以优化首屏加载性能并避免 SSR 兼容性问题。
- 状态管理依赖于 React 本地 Hook（`useState`, `useEffect`）与查询缓存（`useQuery`），实现了项目上下文与筛选条件的联动更新。