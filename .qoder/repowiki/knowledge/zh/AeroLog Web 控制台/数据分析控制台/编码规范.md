- 所有页面组件均声明为 "use client" 以支持浏览器端交互逻辑与 Hook 使用。
- 统一通过 `api.listProjects` 获取项目列表，并利用 `useEffect` 在数据加载后自动选中首个项目作为默认上下文。
- 图表组件均通过 `dynamic(() => import("echarts-for-react"), { ssr: false })` 方式进行异步加载以禁用服务端渲染。
- 时间范围筛选普遍采用 `dayjs` 库配合 Ant Design 的 `RangePicker` 组件，并统一转换为毫秒级时间戳传递给 API。