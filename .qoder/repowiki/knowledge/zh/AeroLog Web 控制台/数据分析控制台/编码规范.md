- 所有页面组件均声明为 "use client"，以支持客户端交互逻辑与动态图表渲染。
- 统一使用 `ProjectPicker` 作为首要筛选条件，并在项目切换时自动重置其他关联的状态变量（如事件、时间范围）。
- 图表组件通过 `dynamic(() => import("echarts-for-react"), { ssr: false })` 方式引入，避免服务端渲染错误。
- 数据请求依赖项（如 `projectId`, `event`）变化时，通过 `useEffect` 或 React Query 的 `enabled` 选项控制查询触发时机。