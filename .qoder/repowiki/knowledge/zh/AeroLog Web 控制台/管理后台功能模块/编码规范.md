- 页面入口文件（page.tsx）保持极简，仅导入并导出对应的特性组件，实现路由与业务逻辑解耦。
- 所有页面均集成 `ProjectSelect` 组件，并在未选择项目时展示 `EmptyState`，确保多租户数据隔离体验一致。
- 统一使用 `MetricCard` 模式在页面顶部展示关键统计指标（如总数、启用数），并配合 `Skeleton` 处理加载状态。
- 列表展示采用响应式设计，桌面端使用 `Table` 组件，移动端自动降级为 `Card` 卡片布局。