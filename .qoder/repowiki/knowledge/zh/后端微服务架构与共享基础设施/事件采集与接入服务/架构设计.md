- 采用 Gin 框架构建 HTTP 服务，入口位于 `cmd/main.go`，通过 `internal/app` 完成依赖注入与生命周期管理。
- 核心业务逻辑封装在 `internal/handler`，利用 `internal/projectcache` 实现基于 PostgreSQL 的项目 Token 内存缓存，以降低数据库查询压力。
- 集成 Kafka Producer 将处理后的事件数据发送至消息队列，同时提供 `/healthz` 健康检查接口。
- 依赖共享包 `pkg` 处理通用模型、消息队列连接及监控指标上报。