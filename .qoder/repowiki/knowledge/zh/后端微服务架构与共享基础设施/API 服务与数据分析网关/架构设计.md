- 采用 Gin 框架构建 RESTful API，通过 `internal/app` 进行依赖注入与生命周期管理。
- 实施双存储架构：PostgreSQL (`pgx`) 负责元数据与配置管理，ClickHouse (`clickhouse-go`) 承载海量事件数据与分析计算。
- 路由层在 `internal/handler` 中按业务域划分（Project, Governance, Analytics），Handler 直接持有数据库连接池执行查询。
- 内置全局中间件处理 CORS、异常恢复及 Prometheus 指标采集（请求耗时与总量）。