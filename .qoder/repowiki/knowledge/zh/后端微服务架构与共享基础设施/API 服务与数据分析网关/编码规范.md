- Handler 方法统一返回 JSON 格式响应，成功时使用 `{"data": ...}` 包装，失败时使用 `{"err": ...}` 包装。
- 所有数据库查询均通过参数化占位符（PostgreSQL 使用 `$N`，ClickHouse 使用 `?`）执行，以防止 SQL 注入。
- 在 Handler 内部定义局部结构体（如 `DebugEvent`, `UserProfile`）用于映射查询结果，并通过 `Scan` 方法填充数据。
- 对于分页或限制返回数量的接口，统一实现 `clampLimit` 逻辑，确保查询量在安全阈值（如 1-500）内。