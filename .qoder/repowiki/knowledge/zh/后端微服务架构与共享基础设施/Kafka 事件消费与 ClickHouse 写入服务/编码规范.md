- 配置加载统一采用 `internal/config.FromEnv()` 模式，通过 `os.LookupEnv` 读取环境变量并提供硬编码默认值。
- 数据库写操作广泛使用 `pgx.Batch` 进行批量 Upsert，以减少网络往返并提升元数据同步性能。
- 指标采集遵循 `metrics.Counter/Histogram` 命名规范，并在关键路径（如消费结果、Flush 耗时、DLQ 计数）打点。
- ETL 富化逻辑（UA 解析、IP 地理位置）被封装在独立的 `internal/etl` 包中，目前以正则匹配和占位符实现为主。