- 所有 Prometheus 指标（Counter/Histogram/Gauge）在创建时自动注册到全局单例 `Registry`，避免分散管理。
- Kafka 生产者采用异步非阻塞发送模式，并在初始化时启动后台 Goroutine 消费 `Errors()` 通道以防止内部阻塞。
- 事件模型采用双层结构：`Event` 承载业务原始数据，`EnvelopedEvent` 承载传输上下文（如 ProjectID, IP），并统一通过 JSON 序列化进行 Kafka 传输。