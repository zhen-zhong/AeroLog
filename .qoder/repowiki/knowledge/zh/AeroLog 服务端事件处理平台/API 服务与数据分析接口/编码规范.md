- Handler 采用结构体封装数据库连接（如 `PG *pgxpool.Pool`），并通过 `Register(*gin.RouterGroup)` 方法集中绑定路由。
- 所有 API 响应统一包装为 `gin.H{"data": ...}` 或 `gin.H{"err": ...}` 的 JSON 格式。
- SQL 查询直接在 Handler 方法内通过 `Query` 或 `QueryRow` 执行，并使用 `rows.Scan` 手动映射到局部定义的 struct。
- 时间范围参数默认回退到过去 7 天，且通过 `atoi64` 等辅助函数处理字符串到时间戳的转换。