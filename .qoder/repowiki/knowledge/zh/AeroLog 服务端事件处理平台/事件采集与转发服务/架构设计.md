- 入口层：`cmd/main.go` 组装 Gin HTTP 服务器、Postgres 连接池及 Kafka Producer，并启动独立指标端口。
- 业务层：`internal/handler/track.go` 实现 `/v1/track` 接口，处理 Gzip 解压、批量解析、Token 鉴权及事件分发。
- 支撑层：`internal/projectcache` 提供基于 Postgres 的 Token 到 ProjectID 映射缓存，降低数据库查询压力；`internal/config` 统一管理环境变量配置。