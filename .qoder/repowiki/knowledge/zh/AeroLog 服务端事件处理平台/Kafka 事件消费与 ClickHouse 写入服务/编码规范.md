- 所有子包（worker, chsink, etl, config）均置于 `internal/` 目录下，严格限制模块外部访问，确保内部实现细节不泄露。
- 采用基于环境变量的配置注入模式，通过 `config.FromEnv()` 统一读取并提供默认值，避免硬编码配置。
- 在关键路径（消息消费、批量写入、DLQ 插入）集成 `pkg/metrics` 埋点，使用 Counter 和 Histogram 监控业务量级与性能耗时。