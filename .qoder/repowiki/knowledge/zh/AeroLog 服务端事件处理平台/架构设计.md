- 采用 Go Workspace (`go.work`) 统一管理 `api`、`collector`、`consumer` 及 `pkg` 四个子模块，实现本地依赖替换与协同开发。
- 所有业务服务均通过 `replace` 指令依赖 `server/pkg`，确保事件模型 (`model/event.go`)、Kafka 生产者封装及监控指标定义的强一致性。
- 形成“采集 (Collector) -> 消息队列 (Kafka) -> 消费入库 (Consumer)”的异步数据流，API 服务则独立提供基于 ClickHouse 的实时分析查询能力。